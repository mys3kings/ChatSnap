{
  "name": "imperion-chat",
  "version": "1.0.0",
  "description": "Secure messaging app",
  "scripts": {
    "start": "firebase serve",
    "deploy": "firebase deploy --only hosting",
    "deploy:prod": "firebase deploy --only hosting --project chatsnap-16189"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
